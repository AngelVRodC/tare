# Local working notes — NOT for the public repo.
# Contains employer-confidential material and an unfiled security report.
# Publish sanitized findings deliberately, never by default.
thoughts/
.atl/

# Local agent tooling (project-scoped Claude Code skills)
.claude/
CLAUDE.md

# Go
tare
*.test
*.out
